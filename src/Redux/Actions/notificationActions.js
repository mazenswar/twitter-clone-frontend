import API from '../../API_CONSTANTS';

const setNotificationsAction = notifications => ({
  type: 'SET_NOTIFICATIONS',
  payload: notifications
});

const getNotificationsFromDB = () => dispatch => {
  fetch();
};

export default {};
